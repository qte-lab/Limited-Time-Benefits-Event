# 问卷换期维护指南

本文说明如何把"答题问卷"从一期切换到下一期（例如从 `2026-08` 换到 `2026-09`）。

## 关键概念

- **`period`（分期）** 是问卷的唯一周期标识，写在 `quiz.json` 顶层。
- **每个用户在每个 `period` 下只能提交一次**。提交记录按 `用户 + period` 存储在 `quiz_submissions.json`。
- **服务端只在启动时读取一次 `quiz.json` 与进度账本**，因此任何数据变更都必须**重启事件服务**才能生效。
- **已移除"日限额"**：现在没有每日上限，用户答对任意一题即按题面 `reward` 发放 GPC，整份问卷可全额领取。
- **前端无需改动**：新前端打开即读取当前 `period` 与 `status` 接口，自动判断本期是否完成、回填历史答案并展示逐题评判。只要 `quiz.json` 的字段结构不变，换期不用动前端。

## 换期操作步骤

### 1. 准备新一期题目
按现有格式准备题目数组（`questions`），每题包含：
- `id`：建议保持 `q1…qN` 连续编号，且**不要与上一期复用相同 id**（避免前端按 id 回填旧答案）。
- `type`：`single`（单选）/`bool`（判断）/`multiple`（多选）。
- `options`：单选/多选为 `['A','B','C','D']`；判断为 `[]`。
- `answer`：单选/多选为正确选项**索引数组或单值**；判断为 `true/false`。
- `reward`：答对该题发放的 GPC 数量。
- `content`：题干（支持 Markdown/公式）。

### 2. 修改 `server/data/quiz.json`
- 把顶层 `"period"` 改成**新值**（约定用 `YYYY-MM`，如 `"2026-09"`）。**这一步是核心**——不改 period，老用户仍被判定为"已提交"而无法重答。
- 用新一期 `questions` 替换原数组。

> 路径：`D:/server/build-gift/server/data/quiz.json`
> （源码副本在 `D:/chronie-app/Limited-Time-Benefits-Event/server/data/quiz.json`，改完建议同步。）

### 3. 清空进度账本
删除上一期的提交与发币记录，避免跨期串数据：
- `quiz_submissions.json` → 写入 `{}`
- `quiz_claims.json` → 写入 `{}`
- 若仍存在旧版 `quiz_submitted.json`，直接删除（新模型已不使用）。

### 4. 重启事件服务
在 `server-go/` 目录下重启 `gift-server.exe`（用新编译的二进制）：

```bat
cd D:/server/build-gift/server-go
taskkill /F /IM gift-server.exe
start "" gift-server.exe
```

重启后服务会重新读取新的 `quiz.json` 与空的账本。

### 5. 验证
```bat
curl -s --noproxy * http://127.0.0.1:3002/api/quiz/questions
```
确认返回的新 `period` 与题目数量正确（如 `period:"2026-09"`）。

用测试账号提交一次，确认：
- 返回 `success:true`、`totalAwarded` 等于答对题数 × `reward`；
- 响应中**不再含有 `balance` 字段**（账户余额已不再获取/展示）；
- 再次提交同一 period 被拒，返回 `alreadySubmitted:true`。

## 常见坑

| 现象 | 原因 | 解决 |
|------|------|------|
| 老用户打开显示"已完成本期答题"，无法重答 | 没改 `period`，服务端内存仍是旧期 | 改 `period` 并重启 |
| 改了题目但页面没变 | 服务没重启，仍读旧 `quiz.json` | 重启 `gift-server.exe` |
| 换新期后旧答案串进来 | 新旧期 `id` 相同，前端按 id 回填 | 新期题目使用全新 `id` |
| 提交返回 401 | 缺少/失效 GPC token | 检查授权登录流程 |
| 提交 0 奖励但显示已完成 | 服务是旧二进制且内存有旧提交态 | 用新二进制重启服务、清空账本 |

## 相关文件清单

- 题目与分期：`server/data/quiz.json`
- 提交记录（用户+分期+答案+评判）：`server/data/quiz_submissions.json`
- 发币幂等记录：`server/data/quiz_claims.json`
- 后端源码：`D:/chronie-app/Limited-Time-Benefits-Event/server-go/quiz.go`
- 前端源码：`D:/chronie-app/Limited-Time-Benefits-Event/android/app/src/main/java/com/chronie/gift/ui/screens/QuizScreen.kt`
- 运行二进制：`D:/server/build-gift/server-go/gift-server.exe`
